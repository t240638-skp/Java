pipeline {
    agent any
stages {
        stage('Clone') {
            steps {
              git branch: 'main', url: 'https://github.com/t240638-skp/Java.git'
            }
        }

    stages {
        stage('Compile') {
@@ -14,3 +20,4 @@ pipeline {
        }
    }
}
}
